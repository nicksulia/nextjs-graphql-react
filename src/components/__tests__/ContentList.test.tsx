import { render } from "@testing-library/react"; import { ContentList } from "../ContentList"; jest.mock("@apollo/client/react", () => ({ useQuery: jest.fn(), useMutation: jest.fn() })); jest.mock("next/link", () => ({ children, href }: any) => <a href={href}>{children}</a>); describe("ContentList", () => { it("should be defined", () => { expect(ContentList).toBeDefined(); }); });
